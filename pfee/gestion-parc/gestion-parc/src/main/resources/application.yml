spring:
  profiles:
    active: local
  ai:
    openai:
      api-key: ${GROQ_API_KEY:gsk_mettez_votre_cle_ici}
      base-url: https://api.groq.com/openai
      chat:
        options:
          model: llama-3.3-70b-versatile
          temperature: 0.3
  datasource:
    url: jdbc:postgresql://localhost:5433/parc
    username: postgres
    password: "13040185"
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
    open-in-view: false

server:
  port: 8081
  servlet:
    context-path: /

logging:
  level:
    org.springframework: INFO
    org.hibernate.SQL: DEBUG
    com.parc: DEBUG

jwt:
  secret: fleetManagementSecretKeyForJWTTokenGenerationAndValidation2024WithExtraLengthToMeet512BitsRequirementForHS512Algorithm
  expiration: 86400000